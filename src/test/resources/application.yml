spring:
  datasource:
    url: jdbc:h2:mem:safedocs_test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    properties:
      hibernate:
        jdbc:
          time_zone: UTC

  mail:
    host: localhost
    port: 0
    username: test
    password: test

logging:
  level:
    org.hibernate.SQL: warn
    org.hibernate.orm.jdbc.bind: warn

jwt:
  secret: "0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef"

auth:
  google:
    client-id: "test-client-id"
